const name = "info";
const description = "To autorized users only: get infos about a user";
const execute = (message, args) => {
  console.log("info command");
};

export { name, description, execute };